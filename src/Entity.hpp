#ifndef ENTITY_HPP
#define ENTITY_HPP

class cTempPlayer {
	public:
		char symbol;
		int posx,
			posy;
		void update (short dx, short dy);
};

class cEntity {
};

class cEntityManager {
};

#endif //ENTITY_HPP
